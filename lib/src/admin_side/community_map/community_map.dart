import 'package:flutter/material.dart';

class AdminCommunityMap extends StatelessWidget {
  const AdminCommunityMap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
